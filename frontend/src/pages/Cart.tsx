import { CartClient, Container } from "@/components";

const Cart = () => {
  return (
    <Container>
      <CartClient />
      <hr className="my-3" />
    </Container>
  );
};

export default Cart;
